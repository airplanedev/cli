name: Deno Echo
slug: deno-echo
description: Echo a name using Deno.
arguments: ["--name", "{{.name}}"]
builder: deno
builderConfig:
  entrypoint: main.ts

parameters:
  - name: Name
    slug: name
    type: string
