
name: Python Echo
description: Echo a name using Python.
arguments: ["--name", "{{.name}}"]
repo: github.com/dummy/dummy
builder: python
builderConfig:
  entrypoint: main.py

parameters:
  - name: Name
    slug: name
    type: string
